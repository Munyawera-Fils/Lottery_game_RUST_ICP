service : {
    // Method to participate in the lottery
    participate : () -> ();

    // Method to draw a winner
    draw_winner : () -> (opt principal);

    // Method to check if the lottery is active
    is_active : () -> (bool);
};
